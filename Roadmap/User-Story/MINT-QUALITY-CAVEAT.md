---
title: MINT quality caveat — two-pass remine
project-id: genesis-mythos-master
---

# MINT quality caveat

**Series:** structure/two-pass machinery validated; series content was **not** proven via a cold PMG-only invent (packs carried prior judgment; archive restored). Cold Phase 0 remains future debt if series content issues appear.

**Children (2026-08-01 rewrite):** walk-facing `summary` fields were regenerated without feedstock glue; evidence lives in `notes`. Status: `children_rewritten_awaiting_grok_validate` / relens awaiting Grok validate.

Poisoned pre-rewrite children archived at:
`Roadmap/User-Story/Versions/children-run--20260801-children-pre-rewrite/`

Grok+user still **validate** child batches before treating them as locked product law.

## Three-layer delivery (2026-08-01) — addition, not remine

PMG now stamps **System + Reference Exemplar + Operator modularity** ([[genesis-mythos-master-goal]]; CDR [[Conceptual-Decision-Records/reference-exemplar-dual-goal-2026-08-01]]; charter [[REFERENCE-EXEMPLAR-CHARTER]]; Phase **6.4**).

**Mint implication:** Catalog stays **capability altitude**. Exemplar graphics / HUD inventory / art bible are **not** new series parents and do **not** require children remine. Swap matrix is System law + later L5 under authorship — not catalog chrome-first parents. Locked `ux_camera_control_envelopes` unchanged. Continue living-world (and later) child batches as before.

Trinity/project pack sync is **later** — vault record first; Grok will not see this frame until publish.

## Walk defs split (2026-08-02)

`MINT-BACKLOG.md` is now a **thin list**. Full Meaning cards are under:

`scopes/<parent>/SERIES.md` and `scopes/<parent>/children-of-<parent>/<child>/WALK.md`

Pass B primary surface: `scopes/<parent>/BATCH-DIGEST.md` (one receipt per [`CHILD-BATCH-VALIDATION`](../../../3-Resources/Second-Brain/Docs/catalog-mint/_shared/CHILD-BATCH-VALIDATION.md)). List directories under `children-of-*` to inventory a batch. Factory `L5.md` is unchanged and is **not** the walk card. Archive of pre-split monolith: `Roadmap/User-Story/Versions/mint-backlog-pre-split-*`.

## Relens (dual-rail / vacuum)

Affinity re-parent applied: camera DM seats → `ux_camera_control_envelopes`; living-world WA → `ux_living_world_continuity` (not mid-game). See `CHILD_SERIES_AFFINITY` in `ux_mint_backlog.py`.

## Catalog Mini-Trinity + L5 affirm (2026-08-03)

First-class frame: [[3-Resources/Second-Brain/Docs/catalog-mint/_shared/CATALOG-MINI-TRINITY|CATALOG-MINI-TRINITY]]. Pass B Meaning children are locked; Loop 2 is **series L5** Pass-B projection (not child L5 batches).

**GMM proof:** thin four series L5s (`ux_combat_play_surface`, `ux_collaborative_table_agency`, `ux_player_character_creation`, `ux_mental_stat_interpretation`) content-affirmed green by Grok on pack `synced_at: 2026-08-03T20:13:58Z`. Board remains yellow on soft `needs_pin`. **Do not set `catalog_signed_at`** until pin policy + full-board attest. Cross-row flags filled on `L5-AFFIRM-STATUS.md`.

**Grok full-board L5 affirm (2026-08-03):** structure green / content still soft; clause-split moments on quiet + power-band parents. **2026-08-04:** rewrote play-verb moments + concrete PoC on `ux_quiet_between_pillars`, `ux_early_game`, `ux_mid_game`, `ux_late_game` (combat bar). Mental-stat already play-verb — left. Pins still unresolved (not waived). Re-affirm yellow subset on next Trinity pack.

## Pin-before-L5 MO (2026-08-04)

First-class order: Pass B lock → **archive premature L5** → pin derive → Grok+user validate → apply_pins → L5 remint (follow-on). Live Pass-B-only L5s archived under `Versions/l5-pre-pin-lens-*` so they cannot poison pin derive. **Remint L5 is out of scope of the pin-propose pass.** See [[3-Resources/Second-Brain/Docs/catalog-mint/_shared/CATALOG-MINI-TRINITY|CATALOG-MINI-TRINITY]] + `PIN-DERIVE-STATUS.md`.
