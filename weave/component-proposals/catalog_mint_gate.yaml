id: catalog_mint_gate
conceptual:
  outcome: Product factory gate — catalog_mint_gate (three operator loops, UX-before-execution).
  summary: Harness-verified law for PMG → product conductor path.
touch:
  primary_paths:
  - scripts/eat_queue_core/weave/user_story/product_factory_pipeline.py
  - scripts/eat_queue_core/weave/user_story/product_factory_ux_context.py
behavior_signals:
  - product_factory.tick blocked_at operator_loop
  - ux_context present on execution RESUME when product_factory_run_id set
status: proposal
# Grok-facing mint instruction law (full legs): catalog_mint.yaml (sibling provisional card).
# This gate remains product-factory tick law; do not treat as Grok feedstock.
pairs_with:
  - catalog_mint

