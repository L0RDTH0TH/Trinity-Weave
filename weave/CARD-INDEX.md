# Trinity card index (auto-generated)

Generated: `2026-06-09T06:11:05Z` — do not hand-edit; regenerated on each `weave_public_sync`.

| id | kind | lock | summary |
|----|------|------|---------|
| `agent_implementation_style` | meta | maintenance_core | This meta card is how agents **implement and integrate** under the **factory + weave-lock** era — not how a single pipel |
| `conceptual_style_guide` | component | maintenance_core | Anti-meta is the spine. All component cards follow `anti_meta` and `field_contract` (outcome, summary, primary_case, opt |
| `config_knob_parity` | meta | maintenance_core | This meta card is **network interlocking for familial profiles**: Config (`Second-Brain-Config`) defines knobs; Rules on |
| `cursor_host_adapter` | meta | maintenance_core | This meta card is the **swappable host adapter** for today's **Cursor IDE** binding. **`harness_runtime_contract`** (#2) |
| `factory_lifecycle_doctrine` | meta | maintenance_core | This meta card is the **Run lifecycle charter**: how factories (queue dispatch, headless paths, roadmap Run, Curator STE |
| `ghost_skill_audit` | component | maintenance_core | Production skills need you at the keyboard. Away-from-keyboard runs rely on proposal stubs as repeat recipes; skill_gap  |
| `harness_runtime_contract` | meta | maintenance_core | This meta card is the **host-agnostic factory runtime**: Layer 0 invokes `python3 -m scripts.eat_queue_core.harness` (an |
| `host_execution_safety_contract` | meta | maintenance_core | This meta card is the execution safety contract for all hosts and pipelines: how destructive work may proceed, distinct  |
| `invariant_registry` | component | maintenance_core | Apex of the policy spine for cross-cutting invariants operators and automation must honor. Local discipline stays on the |
| `l2_predictive_maintenance` | component | maintenance_core | L2 foresight: runtime risk (is now safe to act?) and integration risk (will this change cascade?). Emits narrow-scope or |
| `l2_symbolic_conflict` | component | maintenance_core | L2 pause: compare imminent action against registered invariants. Aligned — continue within bounds. Mismatch — narrow sco |
| `l3_self_heal` | component | maintenance_core | L3 owns the ordered closure recipe after recoverability is established — not the handler catalog, not the board. Precond |
| `l4_adaptive_policy` | component | maintenance_core | L4 owns the unmapped case: record stoppages and inefficiencies, correlate across pseudo-clock audits, propose durable ad |
| `lane_activity` | component | maintenance_core | Single authority for the Run axis after mandatory pre-read reconcile. running means forward momentum — a blocking lock w |
| `lane_status_board` | component | maintenance_core | Obsidian-facing surface over the lane snapshot kernel only — no inferring live Run from receipt history. Health and Run  |
| `launch_registry_reconcile` | component | maintenance_core | Mandatory pre-read before Run classification and board refresh. Closes completed fanout parents and ghost-clears stale i |
| `little_val_structural` | component | maintenance_core | Little val is internal structural honesty. For whatever step is running (mode, action, pipeline — present or future), th |
| `maintenance_honesty_anchor` | meta | maintenance_core | This meta card is the **honesty anchor** for maintenance core and Run receipts: anti-hallucination as **claim classifica |
| `operator_surface_verifier` | component | maintenance_core | Operator surface verifier is the enforcement of do-not-lie-to-the-operator. The operator must be able to see what the sy |
| `persona_atlas` | meta | maintenance_core | This meta card **indexes personas and boundaries** — not one Trinity card per persona. **Architect** — overnight goals,  |
| `public_surface_topology` | meta | maintenance_core | This meta card is the **multi-repo export topology** doctrine. Four surfaces: **curator_private** (full vault backup, ne |
| `recoverable_handlers` | component | maintenance_core | Defines the recoverability class, not today's handler list. A stall is spin without closing the locus. Recoverable means |
| `schedule_event_planes` | meta | maintenance_core | This meta card is the **schedule / pseudo-clock planes** doctrine (Phase 17). **Primary (steady meta):** one **`schedule |
| `skill_gap` | component | maintenance_core | Propose-only half of the overnight pair: scan lane MEMORY for mapped gaps, write one proposal stub per pattern when abse |
| `trinity_card_authoring` | component | maintenance_core | This meta card is the reference for generation on additions and updates — not a chronicle of |
| `trinity_prompt_context` | meta | maintenance_core | This **meta** card (`card_kind: meta`) is the **Prompt ↔ Trinity contract** — not maintenance-only doctrine, not a dupli |
| `trinity_spine_maintenance` | component | maintenance_core | Spine maintenance is the joint between your locked gauges and vehicle sections and the wider corpus of drafts waiting be |
| `trinity_upgrade_integration` | component | maintenance_core | Design-time joint on the maintenance spine: name load, peers, and compensators before ship; adapt after mistakes without |
| `trinity_weave_public_export` | meta | maintenance_core | This meta card binds the **Trinity-Weave** public repo ([L0RDTH0TH/Trinity-Weave](https://github.com/L0RDTH0TH/Trinity-W |
| `vault_layout_naming_doctrine` | meta | maintenance_core | This meta card is the **agent digest** for **where things live** and **what files are called**. Full bodies stay in **Va |
| `weave_governance` | component | maintenance_core | Weave governance is the system's memory backbone for operational truth. It records what was reviewed, what the board loo |

Full YAML: `weave/components/<id>.yaml`

Grok: start at `GROK-START-HERE.md`.
