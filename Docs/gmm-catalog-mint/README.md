# GMM catalog mint (on `main` — what Grok can actually see)

**Bone pilot truth:** Grok’s GitHub connector is OAuth login only. There is **no branch picker**. Grok effectively sees **`main`**. Do not tell anyone to “attach `project/genesis-mythos-master`.”

This folder is the **mint working set published on `main`** so Grok can do the job without a project branch.

| File | Use |
|------|-----|
| `CATALOG-MINT-BLANK.md` | Dialogue contract — **prose first**, then YAML receipt |
| `WHAT-GOOD-LOOKS-LIKE.md` | What “good” mint chat + row means |
| `PMG-EXCERPT.md` | Product feedstock |
| `PIN-INDEX.md` | Legal `conceptual_pin` titles |
| `PIN-EXCERPTS/` | Short Roadmap mirrors for active candidates |
| `slice-catalog.yaml` | Mirror of applied rows (Cursor applies in vault) |

**Cursor** still applies approved rows to `1-Projects/genesis-mythos-master/Roadmap/User-Story/slice-catalog.yaml`, then republishes this pack.
