# Friction check (post-apply)

Before marking a `MINT-BACKLOG` item `done`, answer:

> Does this stub reduce imagined friction for **[persona]** (player / DM / both) in the moment of use?

If no — revise receipt or keep `in_dialogue`. This is judgment, not a machine veto.
